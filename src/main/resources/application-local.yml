# port
server:
  port: 1997
  servlet:
    context-path: /
    session:
      persistent: true
# db
spring:
  datasource:
    url: jdbc:mariadb://103.18.6.89:3306/ui14erqj_chotot
    username: ui14erqj_admin
    password: $*(6RiXgJx4vH@$(
    driver-class-name: org.mariadb.jdbc.Driver
  mvc:
    view:
      prefix: /WEB-INF/jsp/
      suffix: .jsp
  jpa:
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  jackson:
    time-zone: Asia/Ho_Chi_Minh
    date-format: com.fasterxml.jackson.databind.util.ISO8601DateFormat
    serialization:
      write-dates-as-timestamps: true

# uploading
data:
  path: F:/res/travel/

# logging
logging:
  level:
    org.hibernate.SQL: DEBUG
    org.springframework.web.client.RestTemplate: DEBUG

session:
  login: USER_LOGIN
